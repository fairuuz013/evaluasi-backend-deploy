model User {
  id        Int       @id @default(autoincrement())
  name      String
  city      String
  age       Int

  books     Book[]

  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  deletedAt DateTime?
}
